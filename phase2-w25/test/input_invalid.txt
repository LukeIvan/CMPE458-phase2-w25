
if (y==10) a = 3; b = 7; c = 9;   // Missing braces `{}` around statements
if (z==8) { m = 2; n = 4; o = 6;  // Missing closing brace `}`
if (a==1){ x = 100; y = 200; z = 300;   // Missing closing brace `}`
if y==10){ a = 3; b = 7; c = 9;}  // Missing parentheses `()` around condition
if (y=10){ a = 3; b = 7; c = 9;}   // Using `=` instead of `==` in condition
while (y >2 { y = 3; y = 2; y = 1;}  // Missing closing parenthesis `)`
while x >2) { y = 3; y = 2; y = 1;}  // Missing opening parenthesis `(`
while (y >2) { y = 3; y = 2; }  // Missing a statement inside loop
repeat { x = x + 1; until(x > 5); }  // `until` is inside block, not after
repeat { x = x + 1; }  // Missing `until(condition);`
repeat x = x + 1; } until(x > 5);  // Missing opening `{`
print Hello world;  // Missing quotes around string
print 123;  // Print requires a string, but given a number
print x + 5;  // `print` requires a string, but given an expression
print "Mismatched braces;  // Missing closing `"`
int x  // Missing semicolon `;`
x = 5  // Missing semicolon `;`
x = a >  // Incomplete expression (missing second operand)
repeat { while (x < 10) x = 1; } until (x > 10);  // Missing `{}` inside `while`
while (x > 5) { x = 4; x = ; x = 6;}  // Missing value after `x =`
repeat { while (x < 10) { x = 1; } until (x > 10); }  // `until` is inside `while` instead of after `repeat`
